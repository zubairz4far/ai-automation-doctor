# v1.2.1 CI verification

This documentation-only change exists to trigger the repository's pull-request CI against the exact v1.2.1 application code currently on `main`.

No application, model, benchmark, safety, or workflow logic is changed by this file.
